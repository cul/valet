

MAYBE


data = {
  current_user
  CGI params
}

action = process( data )

case action.type
when 'render'
  render action.render_params
when 'redirect'
  redirect action.redirect_params
  
  


  TODO  ???

  precat - availability status
  precat - extra display fields - location name?  summary?

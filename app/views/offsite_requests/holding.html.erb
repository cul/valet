<%= render 'header' %>

Which holding of this record would you like to request?

<br>
<br>

<%= form_tag(new_offsite_request_path, method: 'get') do %>

  <%= hidden_field_tag(:bib_id, @clio_record.key) %>

  <%= render 'bib_form' %>
  <%= render 'holdings_form' %>

  <br>

  <%= submit_tag('Select') %>
  
<% end %>

